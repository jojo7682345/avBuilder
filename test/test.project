inherit compiler = "gcc";

testFunc(arr[]){
    perform{
        lines = [ "1", "1", "2", "2", "3", 4, "4"];

        println(filterUnique(lines));

        writeFileLines("test.txt", filterUnique(lines));
    }
}

test(var1, arr[]) {
    perform{
        testFunc("1", 2, "3");
    }
    return var1;
}