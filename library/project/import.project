
func runFunctionFromProject(projectFile, function) {
    var currDir = currentDir();
    var projectName = fileFullName(projectFile);
    var projectPath = filePath(projectFile); 
    changeDir(projectPath);
    var ret = callExtern(projectName, function);
    changeDir(currDir);
    return ret;
}